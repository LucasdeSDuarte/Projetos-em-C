#include <stdio.h>
#include <stdlib.h>

int main(){
	int valor;
	int total = valor;
	int ced = 50;
	totced = 0;
	printf("Qual eo valor gasto: ");
	scanf("%d",&valor);

	if total >= ced:
    total -= ced
    totced += 1
    else:
    if totced > 0:
    printf("Total de %d cedulas de R$ %d ",totced,ced);
        if ced == 50:
            ced = 20
        elif ced == 20:
            ced = 10
        elif ced == 10:
            ced = 1
        totced = 0
        if total == 0:
